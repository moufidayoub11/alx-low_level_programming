#include "main.h"

/**
 * _isdigit - Function that checks if a given char is a digit (0 - 9)
 *
 * @c: The char to check
 *
 * Return: int, 1 if c is a digit, 0 otherwise
 */

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
		return (1)

	return (0);
}
