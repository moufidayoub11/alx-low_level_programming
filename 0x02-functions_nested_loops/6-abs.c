#include "main.h"

/**
* int _abs - Function that returns the absolute value of a given integer
*
* @n: The int we need to apply abs on
*
* Return: int, the absolute value of n
*/

int _abs(int n);
{
	return (n >= 0 ? n : n * -1);
}
